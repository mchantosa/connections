const Contact= require("../lib/contact");

describe('Contact', () => {
  test('Create contact', () => {
    expect(new Contact(1)).not.toBe(0);
  });
  test('Create contact', () => {
    expect(new Contact(1)).not.toBe(0);
  });
});
